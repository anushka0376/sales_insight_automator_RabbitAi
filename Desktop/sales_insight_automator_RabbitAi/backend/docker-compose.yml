version: '3.8'

services:
  backend:
    build: .
    container_name: sales_insight_backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    # Optional: Use volumes for hot-reloading in development
    # volumes:
    #   - .:/app
    restart: unless-stopped
